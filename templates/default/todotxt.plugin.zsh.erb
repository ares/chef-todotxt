# could not use -h etc
# export TODOTXT_DEFAULT_ACTION=ls

# sort by order in file
export TODOTXT_SORT_COMMAND='env LC_COLLATE=C sort -k 1,1n'

alias t='todo.sh -d /home/<%= node[:user] %>/.todo/config'
alias r='todo.sh -d /home/<%= node[:user] %>/.todo/config-redhat'
